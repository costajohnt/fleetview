---
name: Bug report
about: Something fleetview does wrong
labels: bug
---

**What happened / what you expected**

**Steps to reproduce**
1.

**Environment**
- fleetview commit / version:
- Node version (`node --version`, needs 24+):
- opencode version (`opencode --version`):
- Backend (opencode / claude / copilot):
- OS + terminal:

**Anything in the logs?** (`~/.local/state/fleetview/logs/server.log`, or run with a fresh
`FLEETVIEW_CONFIG_DIR` to isolate). Do not paste secrets or tokens.
